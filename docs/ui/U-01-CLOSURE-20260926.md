# U-01 UI/UX closure — 2026-09-26

Status: **MAINLINE COMPLETE / ACTIVE INTEGRATION DEPENDENCY REMAINS**

Authority:
- repository: `freepass-creator/freepass-data`
- UI branch: `work/data/u-01-ui-ux`
- canonical implementation: `main`
- actual local route: `/console/vehicle-finder`

## 1. U-01 question

> 사용자가 차량이나 데이터를 정확히 다 알지 못해도 원하는 범위까지 자연스럽게 찾을 수 있는가?

For the U-owned presentation layer, the answer is now implemented and continuously guarded.

## 2. Completed U surface

The mainline Finder now covers the full presentation cycle:

1. incomplete-information search entry
2. NEW_CAR / USED_CAR consumption modes
3. F/I-supplied ordered facets
4. candidate list + detail inspection
5. ACTIVE / UNKNOWN / HOLD presentation
6. source / evidence / observed-at / freshness / confidence display
7. explicit COMPLETE / PARTIAL / zero / loading / disconnected / error states
8. last-known-good preservation during refresh/failure
9. F-owned candidate groups
10. F-owned group drilldown options and transition feedback
11. SELECT / INSPECT_ONLY / BLOCKED presentation
12. raw F reason codes + human explanation
13. final selection review before confirmation
14. receipt metadata display
15. receipt CURRENT / RESELECT_REQUIRED presentation
16. mobile 3:7 bottom action boundary
17. desktop list + sticky detail multi-panel
18. keyboard / focus / touch-target / overflow accessibility QA

## 3. Boundary proved

U-01 does **not** own:

- Vehicle Master identity or normalization
- candidate search/ranking semantics
- preferred axis ordering
- group membership or drilldown scoring
- finalization decisions
- receipt issuance or revalidation
- Firestore/RTDB access
- production transport/provider wiring

Browser UI code must not import or invoke F engine functions such as:

- `selectVehicles()`
- `applyVehicleGroupDrilldown()`
- `finalizeVehicleSelection()`
- `issueVehicleSelectionReceipt()`
- `revalidateVehicleSelectionReceipt()`

U receives those outcomes through injected boundaries and renders them.

## 4. Actual-route evidence

The real local Console route is:

`/console/vehicle-finder`

CI `UI Actual Route QA` starts the real local dev server and opens the route in Chromium.

The browser QA now covers:

- desktop and mobile overflow
- new/used mode changes
- group collapse/expand
- drilldown apply/reject
- detail entry/return
- UNKNOWN/HOLD
- final review APPROVED/HOLD
- stale final-review invalidation after a new snapshot
- receipt display
- receipt CURRENT -> RESELECT_REQUIRED
- mobile filter modal
- Tab / Shift+Tab focus trap
- Escape close and focus restoration
- keyboard group disclosure
- detail heading focus
- 3:7 mobile action sizing

Core CI and Canon Guard remain mandatory companions.

## 5. Legacy branch disposition

### `work/data/console-ux`

Audit result: **no unique commits vs main**.

Disposition: obsolete. It must not be used as a development line.

### `work/data/vehicle-finder`

Audit result before retirement:
- diverged far behind main
- retained the old local `preview/vehicle-finder/core.mjs`
- retained historical review/probe/fixture files
- retained the pre-UFEI Finder implementation

Useful UI ideas from that branch were already recovered and superseded in main.

The old `core.mjs` must **not** be revived. It mixes search/candidate semantics into the UI layer and conflicts with the current U/F ownership boundary.

Historical review findings were re-evaluated during U-01 work. Main now addresses their relevant UI concerns, including hierarchy context, evidence display, LKG refresh, partial/zero distinction, mobile bottom actions, grouping, actual-route browser QA and accessibility.

Disposition: obsolete. It must not be used as a development line.

## 6. Remaining dependency

U-01 is not declaring production data integration complete.

The remaining integration dependency is outside U ownership:

- I-01 must provide the authorized real read/finalize/revalidate transport/provider using FreePass Data authoritative sources.
- The standalone/local route remains fail-closed when that provider is absent.
- No demo/fixture vehicle fallback is permitted.
- Tracking issue: **#137**
- Exact handoff / E2E acceptance: `docs/ui/U-01-I-01-VEHICLE-FINDER-E2E-HANDOFF.md`

## 7. Branch policy after closure

Long-lived UI authority:

- `main` = verified truth
- `work/data/u-01-ui-ux` = next U work only, reset to main after every merge

Do not create another long-lived Finder/UI branch.
Do not resume `work/data/vehicle-finder` or `work/data/console-ux`.

If future UI work is required, continue from current `main` through `work/data/u-01-ui-ux` and preserve the U/F/E/I boundaries above.
