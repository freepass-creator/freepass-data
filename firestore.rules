rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Catalog V1 canonical data is server-owned.
    // Browser/mobile consumers must use FreePass Data API/SDK contracts.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
